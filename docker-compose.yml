services:
  unrsschiquito:
    build: .
    container_name: unrsschiquito
    restart: unless-stopped

    ports:
      - "127.0.0.1:8080:8080"

    environment:
      HOST: 0.0.0.0
      UNRSS_NO_AUTH: 1
      # UNRSS_PASSWORD: change
    volumes:
      - ./data:/data
